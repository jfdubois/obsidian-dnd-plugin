# Repository routing instructions

The active automated development workflow is **OpenCode with Qwen**.

For a complete roadmap phase, invoke:

```text
/phase <number>
```

The command must load:

```text
.opencode/skills/dnd-phase-execution/SKILL.md
```

The project-wide engineering rules are authoritative at:

```text
obsidian-dnd-character/AGENTS.md
```

Do not use `.codex/` or `.agents/skills/dnd-phase-execution/` as active workflow instructions. Do not preload the complete documentation set; use `obsidian-dnd-character/CONTEXT_INDEX.md` and bounded reads.
